'use strict'

function readInput() {
    var input = document.getElementById("userInput").value;
    
    if (input != 4) {
        alert("Incorrect!");
    } else {
        alert("Correct!");
    }

}